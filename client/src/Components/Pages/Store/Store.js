import React, {Component} from 'react';

class Store extends Component {

    handleSearchProduct = () => {
        // event.preventDefault();
        //connect to backend to search database for filtered items.
    }

    render() {
        return (
            <div>
                <h1>Store Front</h1>
            </div>
        )
    }
}

export default Store;